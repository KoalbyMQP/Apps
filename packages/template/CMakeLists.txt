# Template CMake
# CMake is a build system generator that creates platform-specific
# build files (like Makefiles on Linux/macOS or Visual Studio projects on Windows).
# This CMakeLists.txt file tells CMake how to compile your C++ code, what libraries
# to link against, and how to organize your project's build process.
# 