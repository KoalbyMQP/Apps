# Template setup.cfg
# What is setup.cfg? setup.cfg is a configuration file that provides a declarative way to specify
# Python package metadata and build options. It's an alternative or complement to setup.py that
# uses a more readable INI-style format. It can define package metadata (name, version, description),
# dependencies, entry points, and tool configurations (like for linters, formatters, and testing tools).